// import React, { useState } from "react";

import TimeDisplay from "./Display";
// import Btns from "./Btns";

const App = () => {
  return (
    <>
      <h1>Stopwatch</h1>
      <TimeDisplay />
      {/* <Btns start={start} stop={stop} wait={wait} reset={reset} time={time} /> */}
    </>
  );
};

export default App;
